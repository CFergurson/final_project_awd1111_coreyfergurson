<main class="container mt-5">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><a href="/account/">Users</a></li>
      <li class="breadcrumb-item active" aria-current="page">{{title}}</li>
    </ol>
  </nav>
   

    <div class="col-md-6">
      <div class="card mb-3 px-3">
        <h2 class="card-title pt-2">{{user.username}}</h2>
        <div class="mb-2 font-weight-bold">Email: {{user.email}}</div>
        <div class="mb-2 font-weight-bold">Bio: {{user.bio}}</div>
      </div>
    </div>

    
  
</main>